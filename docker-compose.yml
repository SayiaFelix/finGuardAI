services:
  fraudguard:
    build: .
    container_name: fraudguard
    ports:
      - "5001:5001"
    env_file:
      - .env
    volumes:
      - ./cache:/app/cache
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped